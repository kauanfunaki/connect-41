// A rota / é tratada por src/app/(app)/page.tsx com o shell autenticado.
// Este arquivo não deve existir simultaneamente — mantido apenas para evitar
// erro de "missing page" caso o build encontre este diretório antes do grupo.
export { default } from "./(app)/page";
